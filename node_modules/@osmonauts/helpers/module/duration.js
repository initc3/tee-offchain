import { Long } from './long';
export function toDuration(duration) {
  return {
    seconds: Long.fromNumber(Math.floor(parseInt(duration) / 1_000_000_000)),
    nanos: parseInt(duration) % 1_000_000_000
  };
}
;
export function fromDuration(duration) {
  return (parseInt(duration.seconds) * 1_000_000_000 + duration.nanos).toString();
}
;